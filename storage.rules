rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Player portraits: readable by any signed-in user (app fetches post-auth).
    // Authored via console/CI only.
    match /players/{file} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
